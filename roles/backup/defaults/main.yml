---
# Backup role defaults
# Configuration for node key/config backups

# Role of the node being backed up: genesis | validator | sentry | rpc
umesh_role: validator

# Base path for backups on the target host
backup_dir_base: /opt/umesh-backups

# Number of backup archives to keep (older ones are removed)
backup_retention: 7
