---
# Validator role defaults
# Joins an existing blockchain as a validator

node_mode: validator

# Chain parameters
chain_id: umesh-testnet-1
denom: uumesh
min_gas_price: "0.0025"
moniker: my-validator

# Join parameters
join_genesis_url: ""
join_genesis_sha256: ""
join_addrbook_url: ""
join_addrbook_sha256: ""
join_public_rpc_fallback: ""
join_sentry_rpc: ""
join_seeds: ""
persistent_peers_for_join: ""

# Keyring
keyring_password: change-me-secure

# Network settings
validator_p2p_bind_ip: 0.0.0.0
validator_rpc_bind_ip: 127.0.0.1

# Resource limits
cpu_limit: "2.0"
memory_limit: 4g
memory_reservation: 2g

# OTEL settings
environment: production

# Data directory
data_dir: data-validator
data_path: "{{ repo_dest }}/{{ data_dir }}"

# Environment file
env_file_dest: "{{ repo_dest }}/.env.validator"