---
# Preflight checks defaults
# Verifies prerequisites and installs missing packages if needed

preflight_checks:
  docker: true
  just: false  # no longer needed: umeshctl is a release binary, docker image is from GHCR
  jq: true
  curl: true
  git: true
  repository: true
  umeshctl: true